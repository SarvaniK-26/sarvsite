import React from "react";
import './DetailsTile.css'

const DetailsTile = ({language}) => {
    return (
        <div>
            <span>{language}</span>
        </div>
    );
};

export default DetailsTile;